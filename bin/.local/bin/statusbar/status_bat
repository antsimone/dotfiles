#!/bin/sh
battery () { 
    acplug=$(cat /sys/class/power_supply/AC/online)
    capacity=$(cat /sys/class/power_supply/BAT0/capacity)
    if [ "$acplug" -eq 1 ]; then
        plug="+"
    else
        plug="-"
    fi  
    echo -e "BAT:$capacity$plug"
}

battery
